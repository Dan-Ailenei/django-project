[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
{% for req in cookiecutter.requirements.split()|sort -%}
{% if "[" in req and "]" in req %}
{{ req[:req.index("[")] }} = {extras = ["{{ req[req.index("[") + 1:req.index("]")] }}"]}
{% else %}
{{ req }} = "*"
{% endif %}
{% endfor -%}


[requires]
python_version = "3.8"
